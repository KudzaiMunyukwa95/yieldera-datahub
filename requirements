# Yieldera DataHub Dependencies
# Install with: pip install -r requirements.txt

# Core Flask framework (likely already installed)
flask==3.0.0
flask-cors==4.0.0

# Data validation and serialization
pydantic==2.5.0

# Google Earth Engine
earthengine-api==0.1.388
google-auth==2.25.2
google-auth-oauthlib==1.2.0

# Geospatial processing
shapely==2.0.2

# Data processing
numpy==1.26.2

# Testing (optional, for development)
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0

# Production server (for deployment)
gunicorn==21.2.0
